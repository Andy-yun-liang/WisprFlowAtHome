## Codebase Patterns
- ICNS binary format: 8-byte file header ('icns' + total size), then entries of 8 bytes (4-byte OSType + 4-byte entry size including header) + raw PNG data; OSType codes: icp4=16, icp5=32, icp6=64, ic07=128, ic08=256, ic09=512, ic10=1024 (required for Retina dock/Finder — do NOT omit)
- SoX is bundled in resources/sox-bundle/ via scripts/bundle-sox.mjs (copies binary + all dylibs, rewrites install names to @loader_path-relative). Regenerated on every prebuild. Do NOT commit sox-bundle to git.
- audio-recorder.ts: resolves bundled sox via `process.resourcesPath` when packaged (`app.isPackaged`), falls back to system sox in dev
- electron-builder extraResources copies resources/sox-bundle → Contents/Resources/sox-bundle at package time
- node-record-lpcm16 CANNOT be used to specify a custom sox path — it hardcodes `cmd = 'sox'` in sox.js regardless of the `recorder` option. Always use direct child_process.spawn instead.
- API keys: stored exclusively in macOS Keychain via keytar (src/main/keychain.ts). Never written to disk, never in env files. Safe to commit all source files.
- Stats + transcript history: src/main/stats-store.ts persists to ~/Library/Application Support/WhisprAtHome/stats.json via electron-store. recordTranscription(text, wordCount, wpm, durationSecs) — text is the first arg.
- Settings UI: pure HTML/CSS/TS (no React/Vue). Icons are inlined Lucide SVG paths (no npm package needed). Nav: Home (stats+history), Settings (all config), About.
- Logo base64 injection: postbuild script inlines resources/logo.png as base64 data URI into out/renderer/settings/index.html to avoid asar path resolution issues
- canvas package (node-canvas) is installed as devDependency for icon generation; uses `createCanvas` from 'canvas'
- prebuild script runs before `npm run build` automatically via npm lifecycle hooks
- This is an Electron + TypeScript app using electron-vite for bundling
- Main process: src/main/ — all OS interaction (hotkey, audio, Whisper API, clipboard, paste, tray, keychain)
- Renderer processes: src/renderer/hud/ and src/renderer/settings/ — pure HTML/TS/CSS, no framework
- Preload scripts: src/preload/ — contextBridge with minimal surface area
- Shared types/constants: src/shared/types.ts and src/shared/constants.ts
- IPC channel names are defined as constants in IPC object in src/shared/types.ts
- Settings persisted via electron-store in src/main/config-store.ts; API key in keytar via src/main/keychain.ts
- Build: `npm run build` runs electron-vite; output goes to out/
- Native addons: uiohook-napi (PTT), keytar (keychain), @nut-tree-fork/nut-js (paste sim)
- @nut-tree/nut-js was 404 on npm — the package is now @nut-tree-fork/nut-js
- uiohook-napi latest is 1.5.4 (not 1.5.5)
- Audio: node-record-lpcm16 spawns SoX subprocess; requires `brew install sox` on macOS
- SoX emits "exit code null" error when forcefully stopped — this is expected, suppress with a `stopping` flag
- Typecheck: `./node_modules/.bin/tsc --project tsconfig.node.json --noEmit`
- node-record-lpcm16 has no @types — there is a manual declaration at src/main/node-record-lpcm16.d.ts
- nut-js keyboard API: pressKey/releaseKey are directly on `keyboard` object, NOT on `keyboard.key`
- macOS requires mic permission via systemPreferences.askForMediaAccess('microphone') before SoX will work in Electron

## 2026-03-14 - US-001
- Extended Settings interface in src/shared/types.ts with whisperModel, language, enabledFillerWords
- Updated DEFAULT_SETTINGS in src/main/config-store.ts with the three new fields (defaults: whisper-1, 'auto', all FILLER_WORDS)
- Updated getSettings() in config-store.ts to return the new fields
- Refactored transcribeAudio/transcribeChunk in src/main/whisper-client.ts to accept a TranscribeSettings argument instead of hardcoding model/language; language 'auto' omits the language param from the API call
- Updated onPttRelease() in src/main/index.ts to read whisperModel and language from getSettings() and pass to transcribeAudio
- **Learnings for future iterations:**
  - whisper-client.ts exports a TranscribeSettings interface; importers should use that type
  - When language is 'auto', omit the language param from the Whisper API call (passing undefined errors out)
---

## 2026-03-14 - US-003 through US-007 (implemented together)
- Redesigned settings window with macOS System Settings-style sidebar navigation
- Updated settings-window.ts: window is now 700×480px
- Rewrote src/renderer/settings/index.html: sidebar nav (200px, #1a1a1a) + content area (#242424), 4 sections
- Rewrote src/renderer/settings/settings.ts: sidebar switching, all section logic
- General section: PTT hotkey capture (keydown listener for modifier combos), HUD segmented control, autostart toggle
- Transcription section: API key password input with masking, model select, language select
- Text Cleanup section: master filler toggle, word chips grid (3 columns) rendered dynamically from FILLER_WORDS
- About section: tray-idle.png icon, version hardcoded, pricing, check-updates button
- Files changed: src/renderer/settings/index.html, src/renderer/settings/settings.ts, src/main/settings-window.ts
- **Learnings for future iterations:**
  - Renderer can import from ../../shared/constants directly (relative path works alongside @shared alias)
  - Word chips rendered dynamically via JS; use classList.toggle for active state
  - Sidebar nav uses data-section attribute on nav items to match section IDs (section-{id})
  - tsconfig.web.json typecheck: `./node_modules/.bin/tsc --project tsconfig.web.json --noEmit`
---

## 2026-03-14 - US-008
- Updated electron-builder.config.ts: added `mac.identity: null` to skip code signing
- Fixed asarUnpack glob patterns to use `**/node_modules/X/**` format (not prefixed with `out/main/`)
- Updated `make` script in package.json to include `--config electron-builder.config.ts`
- Files changed: electron-builder.config.ts, package.json
- **Learnings for future iterations:**
  - electron-builder asarUnpack globs should be `**/node_modules/pkg/**` not `out/main/node_modules/pkg/**/*`
  - `mac.identity: null` bypasses code signing (needed for unsigned local builds)
  - The `--config electron-builder.config.ts` flag is required if the config file is TypeScript
---

## 2026-03-14 - US-002
- Created scripts/generate-icons.mjs using the `canvas` devDependency
- Script draws a dark navy rounded-rect app icon with white mic body and waveform arcs
- Script generates build/icon.icns (ICNS binary with PNG layers at 16/32/64/128/256/512px)
- Script generates resources/tray-{idle,recording,processing,error}.png at 22×22px with grey/red/yellow/orange mic colours
- Added `canvas` to devDependencies and `prebuild` script to package.json
- Files changed: scripts/generate-icons.mjs, package.json, package-lock.json, build/icon.icns, resources/tray-*.png
- **Learnings for future iterations:**
  - ICNS format requires 4-byte OSType + 4-byte length (including 8-byte header) + raw PNG bytes per entry
  - OSType codes: icp4=16px, icp5=32px, icp6=64px, ic07=128px, ic08=256px, ic09=512px
  - canvas.toBuffer('image/png') returns a Node.js Buffer suitable for writing to disk
  - node scripts/generate-icons.mjs can be run standalone without building the Electron app
---
